# 文献检索脚本与检索策略

> 生成日期：2026-09-13
> 用途：规范中英文检索式，明确各数据库检索策略，保证检索可复现、可追溯
> 对应：[调研执行计划.md](调研执行计划.md) 的六个调研方向

---

## 一、检索式总表（按调研方向组织）

### 方向1：PDF表格解析基础（模块1）

**中文检索式（CNKI/万方）**
```
SU=('表格识别'+'表格抽取'+'版面分析') AND SU=('PDF'+'文献'+'文档')
SU=('跨页表格'+'表格复原'+'嵌套表头')
SU=('多模态' AND '表格解析')
```

**英文检索式（Web of Science / ScienceDirect）**
```
TS=("table extraction" OR "table detection" OR "table structure recognition") 
AND TS=("PDF" OR "scientific document" OR "academic paper")

TS=("cross-page table" OR "table continuation" OR "table stitching")

TS=("multi-modal table parsing" OR "table understanding")

TS=("document layout analysis" AND "table")
```

**arXiv检索式**
```
ti:"table extraction" OR ti:"table structure recognition" OR ti:"table understanding"
cat:cs.CV OR cat:cs.CL
（时间范围：近3年，按引用量排序）
```

**GitHub检索式**
```
table extraction scientific PDF
table structure recognition
cross-page table
```

---

### 方向2：LLM结构化抽取与长表约束（模块3）

**中文检索式（CNKI/万方）**
```
SU=('大模型'+'预训练模型') AND SU=('表格抽取'+'结构化抽取'+'信息抽取')
SU=('提示词' AND ('约束' OR '规范') AND '抽取')
SU=('长文档' AND ('截断' OR '上下文窗口'))
```

**英文检索式（Web of Science / ScienceDirect）**
```
TS=("LLM" OR "large language model") AND TS=("table extraction" OR "table parsing" OR "structured extraction")

TS=("long table" OR "long document") AND TS=("truncation" OR "context window" OR "hallucination")

TS=("constrained decoding" OR "structured output" OR "prompt constraint") AND TS=("extraction" OR "parsing")

TS=("hallucination" AND "table parsing")
```

**arXiv检索式**
```
ti:"table" AND ti:"large language model"
ti:"constrained decoding" OR ti:"structured extraction"
```

---

### 方向3：科学数据校验与分层质控（模块4）

**中文检索式（CNKI/万方）**
```
SU=('数据质量' AND ('校验' OR '质控' OR '质量控制'))
SU=('地球化学' AND ('数据' OR '数据库') AND ('校验' OR '质量控制'))
SU=('异常值' AND ('识别' OR '检测') AND '地球化学')
SU=('主量元素' AND ('总和' OR '加和') AND '校验')
```

**英文检索式（Web of Science / ScienceDirect）**
```
TS=("scientific data" AND "quality control") AND TS=("geochemical" OR "geological" OR "earth science")

TS=("data validation" OR "data quality checking") AND TS=("geochemical data" OR "rock geochemistry")

TS=("major element" AND ("total" OR "sum") AND "100" AND "wt%")

TS=("hierarchical quality control" OR "constraint-based data cleaning")

TS=("rare earth element" OR "REE" AND "quality check")
```

**数据库文档检索**
```
GEOROC: 数据提交指南、质控说明页
PetDB: Data Submission Guide、QA/QC documentation
EarthChem: Data Standards、Quality Control
```

---

### 方向4：模板演化与闭环优化（模块5）

**中文检索式（CNKI/万方）**
```
SU=('自适应' AND ('模板' OR '规则') AND ('抽取' OR '解析'))
SU=('失败样本' OR '错误驱动' OR '反思')
SU=('Agent' AND ('信息抽取' OR '数据处理'))
```

**英文检索式（Web of Science / arXiv）**
```
TS=("adaptive information extraction" OR "adaptive IE")

TS=("error-driven" OR "failure-driven") AND TS=("template" OR "rule" OR "prompt")

TS=("reflexion" OR "self-refine" OR "reflection") AND TS=("agent" OR "LLM")

TS=("prompt optimization" OR "automatic prompt engineering" OR "APE")

TS=("lifelong" OR "incremental") AND TS=("information extraction")
```

---

### 方向5：地学文献数据库工程（模块6）

**中文检索式（CNKI/万方）**
```
SU=('地球化学数据库' OR '地学数据库')
SU=('文献数据' AND ('汇交' OR '录入' OR '建库'))
SU=('数据标准化' AND ('地学' OR '地球化学'))
```

**英文检索式（Web of Science）**
```
TS=("geochemical database" AND ("compilation" OR "construction" OR "literature"))

TS=("data harmonization" OR "schema alignment") AND TS=("geochem*")

TS=("GEOROC" OR "PetDB" OR "EarthChem")
```

---

## 二、数据库检索策略

### CNKI / 万方（中文核心）
- **范围**：北大核心 + CSSCI，近10年
- **学科**：地质学、计算机应用、图书馆学（表格识别类）
- **检索字段**：主题检索为主，摘要检索补充
- **策略**：先宽后窄——先主题词粗检，再用"被引排序"筛高影响力
- **重点期刊**：《地学前缘》《岩石学报》《地球化学》《地球科学进展》、计算机类核心期刊

### Web of Science（英文权威）
- **范围**：SCI/SCIE，近10年
- **分类**：Geochemistry & Geophysics、Computer Science、Geology
- **策略**：
  1. 每个检索式执行后按"被引频次"降序排列
  2. 重点看高被引（>50）+ 近3年新作
  3. 用"Related Records"扩展
- **重点期刊**：Computers & Geosciences（⭐地学计算机方法顶刊，必查）

### ScienceDirect
- **范围**：Elsevier 全文
- **策略**：先用WoS定位论文，再到ScienceDirect读全文；直接用SD检索时限定期刊

### arXiv（最新预印本）
- **范围**：cs.CV、cs.CL、cs.AI，近3年
- **策略**：
  1. 按检索式搜标题
  2. 按引用量/影响力排序
  3. 重点追踪：Table Transformer相关、多模态表格解析、Document AI
- **优势**：能发现尚未发表的最新方法，开题时显得前沿

### GitHub（工程实现）
- **策略**：
  1. 用检索式搜仓库，按star排序
  2. 重点读README的"Limitations/Known Issues"章节
  3. 读Issue区——**开发者记录的现实痛点是最真实的失败案例来源**
- **必看仓库**：MinerU、TableTransformer、PaddleOCR、Camelot

---

## 三、检索执行流程（标准化操作）

```
第1步：执行检索式 → 导出题录（含摘要）
第2步：初筛（读标题+摘要）→ 标记相关/不相关
第3步：精读清单（按优先级排序）：
    P0 = 直接相关（表格抽取+校验/约束/闭环）
    P1 = 方法可借鉴（多模态解析、自适应抽取）
    P2 = 背景参考（地学数据库、数据质控）
第4步：精读并填写《研究现状对比表》对应行
第5步：提取该文献的"没做什么" → 记录到研究缺口清单
第6步：每周汇总，对照六大模块查漏补缺
```

### 文献管理建议
- 使用 Zotero / NoteExpress 管理题录
- 标签体系：`#模块1 #模块3 #失败案例 #领域校验 #对比基线`
- 每篇精读文献输出三句话：做了什么 / 怎么做的 / 没做什么

---

## 四、检索进度跟踪表

| 方向 | 检索式已执行 | 题录数 | 精读数 | 已填对比表 | 状态 |
|------|-------------|--------|--------|-----------|------|
| 方向1：表格解析 | 🔲 | 0 | 0 | 0 | 待检索 |
| 方向2：LLM约束抽取 | 🔲 | 0 | 0 | 0 | 待检索 |
| 方向3：分层校验 | 🔲 | 0 | 0 | 0 | 待检索 |
| 方向4：闭环优化 | 🔲 | 0 | 0 | 0 | 待检索 |
| 方向5：数据库工程 | 🔲 | 0 | 0 | 0 | 待检索 |

---

## 五、目标数量（开题支撑所需）

| 类型 | 目标 | 说明 |
|------|------|------|
| 中文核心论文 | 15-20篇 | 方向1、2、4为主 |
| 英文SCI论文 | 25-30篇 | 各方向均衡 |
| arXiv预印本 | 10-15篇 | 前沿方法 |
| 开源工具文档 | 9个工具 | 对比基线 |
| 数据库文档 | 3-4个 | GEOROC/PetDB等 |
| 精读文献 | 20-25篇 | 能写出"做了什么/没做什么" |

**汇总标准**：对比表中每个方向至少填满5行，研究缺口每条至少2篇文献支撑。
