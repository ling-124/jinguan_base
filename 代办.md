# 镜观项目代办清单

生成日期：2026-09-24

## 当前状态

- 已根据项目文档建立 SQLite 数据库：`database/jinguan.sqlite3`
- 已建立建库脚本：`database/schema.sql`
- 已建立默认种子数据：`database/seed.sql`
- 已建立只读数据库前端：`serve_frontend.py` + `frontend/`
- 已建立 Python 项目骨架：`jinguan/`
- 已实现 SQLite Repository：`jinguan/repository/sqlite.py`
- 已实现本地 ArtifactStore：`jinguan/artifact_store/local.py`
- 已实现本地 PDF 导入入口：`jinguan/pipeline/acquisition.py`
- 已建立基础单元测试：`tests/test_repository_artifacts.py`
- 原始 Markdown / PPT 项目文件未删改

## 总目标

完成一个可跑通的最小闭环：

```text
PDF 导入
→ 表格解析
→ 模板路由
→ 结构化抽取
→ 分层校验
→ 入库
→ 前端查看
→ 失败回流 / 审核
```

## P0：先完成系统地基

### 1. 建立正式 Python 项目骨架

建议结构：

```text
jinguan/
  config.py
  repository/
  artifact_store/
  pipeline/
    acquisition.py
    parser.py
    router.py
    extractor.py
    validator.py
    gold.py
  api/
  tests/
```

交付物：

- [x] `jinguan/` 源码目录
- [x] 基础配置加载逻辑
- [x] 单元测试入口

### 2. 实现 Repository 层

业务代码统一通过 Repository 访问数据库，避免各模块直接写 SQL。

首批方法：

- `create_paper(...)`
- `get_paper(...)`
- `create_run(...)`
- `update_run_status(...)`
- `get_run(...)`
- `save_parsed_table(...)`
- `save_route_decision(...)`
- `save_extracted_table(...)`
- `save_extracted_records(...)`
- `add_validation_issue(...)`
- `add_run_event(...)`

交付物：

- [x] `SQLiteRepository`
- [x] Repository 测试
- [x] 基础写入/读取示例

### 3. 实现 ArtifactStore

统一管理文件资产，禁止业务代码直接拼接 `/srv/jinguan-data/...`。

需要支持：

- 保存 raw PDF
- 保存 `work/<run_id>/parsed.json`
- 保存 `work/<run_id>/route.json`
- 保存 `work/<run_id>/extracted.json`
- 保存 `work/<run_id>/validation.json`
- 保存 evidence
- 保存 review 文件
- 保存 gold 版本

硬规则：

- 数据库只保存相对 `storage_key`
- 不保存绝对路径
- raw 目录只追加，不原地修改
- Gold 数据集必须版本化

交付物：

- [x] `ArtifactStore`
- [x] 本地开发路径适配
- [x] storage_key 与物理路径互转测试

## P1：跑通第一条真实流水线

### 4. PDF 导入模块

先支持本地 PDF 导入。

流程：

```text
local PDF
→ 校验文件存在
→ 校验 PDF 头
→ 计算 SHA-256
→ 保存 raw/pdfs/sha256/...
→ 写入 papers / pdf_assets / acquisition_manifests
```

交付物：

- [x] `import_local_pdf(...)`
- [x] PDF SHA-256 去重
- [x] 导入日志写入 `run_events`

### 5. 表格解析模块

第一版先降低难度，支持手工指定页码或区域。

建议工具：

- PyMuPDF
- pdfplumber
- 后续再接 MinerU

目标输出标准 `ParsedTable`：

```text
table_id
run_id
paper_id
page_start
page_end
bbox_json
markdown
image_storage_key
expected_rows
expected_columns
parser_name
parsed_payload
```

交付物：

- [x] `ParsedTable` 数据对象
- [x] 基础 Markdown 表格解析
- [x] 写入 `parsed_tables`

### 6. 模板路由模块

先实现规则版路由：

- 横向样品-字段表格 → `FORMAT_A`
- 纵向属性-样品表格 → `FORMAT_B`
- 无法判断 → `UNKNOWN`

交付物：

- [x] `route_table(...)`
- [x] 写入 `route_decisions`
- [x] 在前端可查看路由结果

## P2：抽取、校验、入库

### 7. 结构化抽取模块

输入：

```text
ParsedTable
+ RouteDecision
+ geochemistry-v1 schema
+ expected_rows / expected_columns
```

输出：

```text
ExtractedTable
ExtractedRecord[]
ExtractedValue[]
```

第一版约束：

- 显式传入 N / M
- 禁止 `...`
- 空值统一 `null`
- 保留所有样品行
- 保留字段顺序

交付物：

- 抽取器接口
- 一个可替换的 LLM 抽取适配器
- 一个 mock 抽取器用于测试

### 8. 分层校验模块

三层校验：

```text
结构层：行数、字段数、JSON 合法性、记录完整性
格式层：字段名、单位、数值格式、列映射
领域层：主量元素总和、微量元素范围、REE、同位素范围
```

第一版先实现：

- `row_count_matches_expected`
- `field_count_matches_expected`
- `json_valid`
- `known_field_names`
- `numeric_values_parseable`
- `major_element_sum_reasonable`

交付物：

- `ValidationResult`
- `ValidationIssue`
- 写入 `validation_results` / `validation_issues`
- `passed` / `needs_review` / `failed` 状态判断

### 9. Gold 数据集生成

规则：

- 只有 `passed` 结果能进入 Gold
- Gold 必须版本化
- 不覆盖旧版本

交付物：

- `publish_gold_version(...)`
- 写入 `dataset_versions`
- 写入 `gold_records`
- 导出 CSV / JSON

## P3：前端增强

### 10. 前端增加业务视图

现有前端是数据库浏览器，后续需要增加面向项目流程的页面：

- 文献列表
- PDF 资产列表
- Run 详情页
- 表格解析结果页
- 校验问题页
- Review 队列
- Gold 数据集页

交付物：

- Run 详情页可看到完整证据链
- 校验问题可按严重度筛选
- Gold 版本可下载

### 11. 增加导入与执行按钮

前端后续应支持：

- 导入本地 PDF
- 创建 run
- 执行解析
- 执行抽取
- 执行校验
- 推送到 review / gold

交付物：

- 后端 API
- 前端操作按钮
- 状态实时刷新

## P4：实验与论文支撑

### 12. 准备真实测试集

先准备 5 篇代表性地学 PDF：

- 至少 1 篇跨页续表
- 至少 1 篇多级嵌套表头
- 至少 1 篇大量缺测值
- 至少 1 篇超长样品表
- 至少 1 篇中文文献

交付物：

- `DOC-001` 至 `DOC-005` 登记卡
- 原始 PDF 入库
- 表格类型、页码、样品数、字段数记录

### 13. 建立失败案例库

按 `失败案例收集方案.md` 执行。

交付物：

- 失败案例卡
- 失败截图 / evidence
- 工具输出对比
- 问题归因
- 对应模块改进建议

### 14. 对比实验

基线工具：

- Camelot-py
- Tabula-py
- pdfplumber
- PyMuPDF
- TableTransformer
- PaddleOCR / PP-Structure
- MinerU
- GPT-4V / GPT-4o
- Qwen-VL

交付物：

- 工具能力矩阵
- 成功率 / 失败率统计
- 长表截断率
- 空值保留率
- 字段正确率

## 推荐下一步

优先做：

```text
P0-1 项目骨架
P0-2 Repository 层
P0-3 ArtifactStore
```

原因：

- 这是后续所有模块的地基。
- 做完后，解析、抽取、校验都可以稳定写入数据库。
- 前端也能从“数据库浏览器”升级成真正的系统界面。
